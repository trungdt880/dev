#!/usr/bin/env bash
set -euo pipefail

# HTTPS so a fresh box without SSH keys can clone.
if [ -d "$HOME/fzf" ]; then
    echo "[skip] fzf already cloned at $HOME/fzf"
else
    git clone https://github.com/junegunn/fzf.git "$HOME/fzf"
fi
"$HOME/fzf/install" --all --no-update-rc
